#!/bin/bash

factor='1.2'

case $1 in
	up)
		light -S $(echo $(light) '*' $factor '+0.01' | bc -l)
		light
		;;
	down)
		light -S $(echo $(light) '/' $factor | bc -l)
		light
		;;
	*)
		echo invalid command > /dev/stderr
		exit 1
		;;
esac
